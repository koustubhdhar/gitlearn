git clone "https link"
git status
ls 
ls -a
git add file name 
git add.
git commit -m "msg"
git push origin main
// if we want to create new repo
git init
git remote add origin 
"https link"
git remote -v
